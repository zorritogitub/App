{
  "name": "Te amow",
  "version": "1.0.0",
  "description": "Quieres ser mi novia❤️",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/ && cp amor.mp3 www/",
    "start": "npx cap open android",
    "android": "npx cap add android && npx cap sync",
    "ios": "npx cap add ios && npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/cli": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  }
}
